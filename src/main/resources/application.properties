
# Security
metis.cas.url=https://authext-asb
metis.cas.url.login=https://authext-asb/login
metis.cas.url.logout=https://authext-asb/logout

metis.cas.service.url=http://localhost:8080
#metis.cas.service-url=http://127.0.0.1:8080
#metis.cas.service-url=https://127.0.0.1:8443

metis.service.secure.path=/securePlace
metis.secure.place.allowed.domains=localhost,127.0.0.1

metis.application.roles==UTBD,MKSA,ICON,UTND,UTF0,UTF1

# Custom WebMvcConfig
## metis.cors.allowed-origins (default : http://localhost:4200,http://localhost:8080)
metis.cors.allowed-origins=http://localhost:4200

# metis.cors.applied-on-url-patterns (default : /api/**,/j_spring_security_logout)
#metis.cors.applied-on-url-patterns=/**

#metis.contextual-resource-keys=metis.cas.login-url,metis.cas.logout-url:cas.url.logout

#metis.session.cookie-same-site=None
#metis.session.redis-cas-mapping-storage-timeout-in-seconds=7200

# Attribute "Secure" on cookie. Need by Chrome 80, with SameSite None
#server.servlet.session.cookie.secure=true

## Attributes for https configuration
# The format used for the keystore. It could be set to JKS in case it is a JKS file
#server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
#server.ssl.key-store=classpath:keystore/localhost.p12
# The password used to generate the certificate
#server.ssl.key-store-password=password
# The alias mapped to the certificate
#server.ssl.key-alias=localhost

# server.port 8443 because /j_spring_cas_security_check always redirect to 8443 if url has https ...
#server.port=8443

# context path for call by frontend Angular
## PROBLEME : s'applique aussi sur les servlets => on a fait autrement, via la conf Mvc
#server.servlet.context-path=/api



## Datasource HSQLDB
spring.datasource.driver-class-name=org.hsqldb.jdbc.JDBCDriver
spring.datasource.url=jdbc:hsqldb:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
# For INSERT queries in data.sql, without ID, expecting ID generation
spring.jpa.hibernate.use-new-id-generator-mappings=false

## Embedded Server configuration
# Compression
server.compression.enabled=true

## Spring Boot Configuration
spring.main.banner-mode=OFF

## Metrics
# Enable metrics for all MVC requests
management.metrics.web.server.request.autotime.enabled=true
# Enable metrics for Hibernate
spring.jpa.properties.hibernate.generate_statistics=true

# Enable actuator metrics exposure on web
management.endpoints.web.exposure.include=info,health,metrics


#### Tests jasypt metis-boot
non_encrypted_prop=non_encrypted_value
# encrypt.bat input="PassWord" password=test_secret algorithm=PBEWithMD5AndTripleDES
encrypted_prop=ENC(q2YWUvrwUS2hXCEHAbaEjesEAhWNFLKR)


####  Spring session redis
## spring.session.store-type (values : none (pour desactiver), redis)
spring.session.store-type=none
# on desactive car cet indicateur est actif m�me si spring.session.store-type=none
management.health.redis.enabled=false

#spring.session.redis.namespace=metis-ng-showcase-boot:dev

## Redis mode standalone
#spring.redis.host=localhost
#spring.redis.password=
#spring.redis.port=6379

## Redis mode sentinel
#spring.redis.sentinel.master=a0494
#spring.redis.sentinel.nodes=redis-sentinel-hprd-1:20494,redis-sentinel-hprd-2:20494,redis-sentinel-hprd-3:20494
#spring.redis.password=n8PrY9aN2


#### LOGS
logging.level.org.springframework.security=INFO

